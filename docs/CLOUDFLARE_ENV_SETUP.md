# Cloudflare / Perplexity Env Setup (sanitized)

Set environment variables in your deployment platform or `.env` file.

- `PERPLEXITY_API_KEY=REDACTED`

Do NOT commit your real API keys. Use secret managers or GitHub Actions secrets.
